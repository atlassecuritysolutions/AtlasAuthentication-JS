{
  "name": "atlas-authentication-electron-example",
  "productName": "Atlas Auth Electron Example",
  "version": "1.0.0",
  "description": "Atlas Authentication SDK - Electron desktop example. atlassecurity.site",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start":     "electron .",
    "build:exe": "electron-packager . \"Atlas Auth Electron Example\" --platform=win32 --arch=x64 --out=\"../../../../- Builds\" --overwrite --asar.unpackDir=\"node_modules/{koffi,atlas-authentication}\" --extra-resource=\"../Atlas SDK/Atlas.dll\""
  },
  "devDependencies": {
    "electron":          "^32.0.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "atlas-authentication": "file:../Atlas SDK",
    "koffi":                "^2.9.0"
  }
}
